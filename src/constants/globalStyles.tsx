import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    screenWrapper: {
        minHeight: "100%",
        backgroundColor: "white",
        padding: 16
    }
})